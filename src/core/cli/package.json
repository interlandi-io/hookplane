{
    "name": "@hookplane/cli",
    "type": "module",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "test": "doppler run -- vitest",
        "__test:run": "doppler run -- vitest run",
        "test:watch": "doppler run -- vitest --watch",
        "check-types": "tsc --noEmit",
        "lint": "eslint . --max-warnings 0"
    },
    "exports": {
        ".": "./src/index.ts"
    },
    "dependencies": {
        "@hookplane/core": "workspace:*",
        "neverthrow": "^8.2.0",
        "ts-morph": "^27.0.2"
    },
    "devDependencies": {
        "@hookplane/stripe": "workspace:*",
        "@repo/eslint-config": "workspace:*",
        "@repo/typescript-config": "workspace:*",
        "@types/node": "^22.15.3",
        "@types/picomatch": "^4.0.3",
        "vite": "^8.0.0",
        "vitest": "^4.1.2"
    }
}
